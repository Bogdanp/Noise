import Foundation

public struct NoiseBoot {
  public static let petiteURL = Bundle.module.url(forResource: "boot/petite", withExtension: "boot")!
  public static let schemeURL = Bundle.module.url(forResource: "boot/scheme", withExtension: "boot")!
  public static let racketURL = Bundle.module.url(forResource: "boot/racket", withExtension: "boot")!
}
